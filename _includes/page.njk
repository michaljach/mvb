<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="{{ '/style.css' | url }}" />
    <title>{{ title }}</title>
  </head>
  <body>
    <main class="post">
      <nav><a href="{{ '/' | url }}">&larr; Home</a></nav>
      <article>
        <h1 class="post-title">{{ title }}</h1>
        {{ content | safe }}
      </article>
    </main>
  </body>
</html>
